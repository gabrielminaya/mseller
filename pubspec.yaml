name: mseller
description: A new Flutter project.

publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.3
  get_it: ^7.2.0
  dartz: ^0.10.0
  path: ^1.8.0
  path_provider: ^2.0.5
  
  sqflite: ^2.0.0+4
  
  bloc: ^7.2.1
  flutter_bloc: ^7.3.0
  
  search_page: ^2.0.0+1
  animations: ^2.0.1
  auto_animated: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
  